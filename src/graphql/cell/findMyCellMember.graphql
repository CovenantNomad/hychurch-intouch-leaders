query findMyCellMember($id: ID!) {
  user(id: $id) {
    id
    name
    phone
    grade
    isActive
    birthday
    gender
    address
    description
    roles
    registrationDate
    cell {
      id
      name
    }
  }
}
