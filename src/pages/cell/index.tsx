import React from 'react'
//components
import Layout from '@components/Atoms/Layout/Layout'

interface CellProps {}

const Cell = ({}: CellProps) => {
  return <Layout>Cell</Layout>
}

export default Cell
